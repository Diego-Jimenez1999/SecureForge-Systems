# Base profile (dev). Override with env vars: DB_URL, DB_USERNAME, DB_PASSWORD.
db.url=jdbc:mysql://localhost:3306/secureauth_db?sslMode=DISABLED&serverTimezone=UTC
db.username=dev_user
db.password=dev_password_change_me

# Security
security.maxFailedAttempts=5
